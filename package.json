{
  "name": "flip-toonz",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test",
    "flip": "bun run packages/engine/cli.ts",
    "solo": "bun run packages/engine/tui.ts",
    "web": "cd apps/web && bun run dev",
    "server": "cd apps/server && bun run index.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}
