{
  "name": "flip-toonz",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": ["apps/web"],
  "scripts": {
    "test": "bun test",
    "web": "cd apps/web && bun run dev",
    "server": "cd apps/worker && bunx wrangler dev",
    "lint": "oxlint"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.0.0",
    "@playwright/test": "^1.62.1",
    "@types/bun": "^1.3.14",
    "oxlint": "^1.79.0",
    "vitest": "^4.1.11",
    "wrangler": "^4.125.0"
  }
}
