{
  "name": "flip-toonz",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": ["apps/web"],
  "scripts": {
    "test": "bun test",
    "web": "cd apps/web && bun run dev",
    "server": "cd apps/server && bun run index.ts",
    "lint": "oxlint"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/bun": "^1.3.14",
    "oxlint": "^1.79.0"
  }
}
